 <!-- ALSO -->
    <section class="also center">
        <h2 class="also__title">you may like also</h2>
        <div class="also__box">
            {% for item in recomends %}
                {% include "blocks/catalog_item.twig" %}
            {% endfor %}
        </div>
    </section>
